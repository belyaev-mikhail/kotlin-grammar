package me.two.you

data class Classo(val x: Int): @Anno Base(), @Anno IA, @Anno IB {}

val z = 2 in listOf(12) is Boolean in listOf(true, false) is Boolean

val z = - @Annotation 2

fun x() {
    while(x > 0) x = --x
}
